<link rel="stylesheet" href="../css/style.css">
<header></header>
{{#if reason}}
<div class="card w-75">
  <div class="card">
    <img class="bigImage" src="/Winnie-the-Pooh-Dinner-1920x1408 (1).jpg">
    <div class="card-block">
      <h4>{{reason.name}}{{info}}{{dataCountry.name}}</h4>
      <h4>{{dataReason}}- Just in time!!!!!!!!</h4>
      <div><h5>{{data}}</h5></div>
    </div>
  </div>
</div>
</div>
{{/if}}


{{!-- <script>
  
  const divForFormEdit = document.getElementsByName('divForTranslate')[0];
  const translateButton = document.getElementsByName('translateButton')[0];



  translateButton.addEventListener('click', async e => {
   
      const resp = await fetch(`/translate`, {
        method: 'PUT',
        headers: {
          'Content-type': 'application/json',
          accept: 'application/json',
        },
        body: JSON.stringify({
          name: e.target.name.value,
          location: e.target.location.value,
          date: e.target.date.value,
        })
      });
      const divForstatus = document.getElementsByName('divForStatus')[0];
      const data = await resp.json();
      alert(data.status);
  })

</script> --}}